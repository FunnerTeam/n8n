# Simple Dockerfile using official n8n image with correct Node.js version
FROM n8nio/n8n:1.94.1

# Set environment for development
ENV NODE_ENV=development
ENV N8N_PORT=5678

# Expose port
EXPOSE 5678

# Use the default entrypoint from n8n image
CMD ["n8n"] 