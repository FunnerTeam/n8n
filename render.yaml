services:
  - type: web
    name: n8n-funner-staging
    env: docker
    dockerfilePath: ./Dockerfile.render
    dockerContext: .
    plan: starter
    region: oregon
    branch: basic-deploy
    autoDeploy: false
    runtime: docker
    buildCommand: ""
    startCommand: ""
    buildFilter:
      paths:
        - packages/**
        - docker/**
        - "*.json"
        - "*.js"
        - "*.ts"
    envVars:
      - key: NODE_ENV
        value: development
      - key: N8N_HOST
        sync: false
      - key: N8N_PORT
        value: 5678
      - key: N8N_PROTOCOL
        value: https
      - key: WEBHOOK_URL
        sync: false
      - key: GENERIC_TIMEZONE
        value: Asia/Jerusalem
      - key: N8N_METRICS
        value: false
      - key: DB_TYPE
        value: sqlite
      - key: N8N_LOG_LEVEL
        value: debug
      - key: N8N_RUNNERS_ENABLED
        value: false
    healthCheckPath: /healthz
